ry {
    $conn = new PDO("sqlsrv:server = tcp:tagitsqlserver.database.windows.net,1433; Database = tagitsqldb", "pbansal", "{your_password_here}");
    $conn->setAttribute(PDO::ATTR_ERRMODE, PDO::ERRMODE_EXCEPTION);
}